<p><%= t('.greeting', recipient: @email) %></p>

<p><%= t('.instruction') %></p>

<div class="flex justify-start mt-6">
    <%= link_to t('.action'), confirmation_url(@resource, confirmation_token: @token, locale: @resource.locale), 
        class: 'p-2 px-4 bg-blue-700 text-white rounded-lg'%>
</div>
