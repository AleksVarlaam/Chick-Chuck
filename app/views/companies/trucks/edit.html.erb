<% currently_at t('truck.edit_btn') %>
<% provide :page_title, t('truck.edit_btn') %>
<% provide :main_title, "#{t('truck.edit_btn')} #{@truck.number}" %>
<% provide :parent_title, t('truck.my_trucks') %>
<% provide :parent_path, companies_trucks_path %>
<% provide :nav_title, "#{t('truck.edit_btn')} #{@truck.number}" %>
<% provide :nav_path, truck_path(@truck) %>

<!-- Trucks Cards -->
<div class="w-full mx-auto">
    <%= render 'form' %>
</div>

<!-- All trucks link -->
<%= link_to t('truck.my_trucks'), companies_trucks_path, 
    class: "mt-6 rounded-lg py-3 px-5 bg-gray-100 hover:bg-gray-200 inline-block font-medium"
%>
   

