<!-- Feedback -->
<section class="flex justify-center" id="feedback">
    <div class="pt-6 mx-auto text-center">
        <!-- Title -->

        <!-- Link -->
        <div class="flex justify-center">
            <%= link_to about_path(anchor: 'comments_title'), class: "py-2 px-3 font-semibold rounded-lg text-white bg-gradient-to-r from-blue-500 via-blue-600 to-blue-700 hover:bg-gradient-to-br dark:text-gray-900 dark:bg-violet-400 cursor-pointer flex items-center" do %>
                <%= heroicon 'chat-bubble-bottom-center-text', class: 'w-5 h-5 mr-2'%>
                <%= t('review.add')%>
            <% end %>
        </div>
    </div>
</section>