<div class="px-4 py-16 mx-auto sm:max-w-xl md:max-w-full lg:max-w-screen-xl md:px-24 lg:px-8 lg:py-20 " id="top_features">
	<%=tag.h2 t('home_page.top_features.title'), class: "mb-8 text-3xl font-bold leading-none text-center"%>
	<ul class="grid gap-3 md:grid-cols-2 lg:grid-cols-3">
        <!-- Combining -->
		<li class="flex items-center space-x-2">
            <%= heroicon 'check-circle', class: "w-6 h-6 flex-none #{I18n.locale == :he ? 'ml-2' : ''}" %>
            <%= tag.span t('home_page.top_features.combining')%>
		</li>
        <!-- Reviews -->
		<li class="flex items-center space-x-2">
			<%= heroicon 'check-circle', class: "w-6 h-6 flex-none #{I18n.locale == :he ? 'ml-2' : ''}" %>
			<%= tag.span t('home_page.top_features.reviews')%>
		</li>
        <!-- Calculator -->
		<li class="flex items-center space-x-2">
			<%= heroicon 'check-circle', class: "w-6 h-6 flex-none #{I18n.locale == :he ? 'ml-2' : ''}" %>
			<%= tag.span t('home_page.top_features.calculator')%>
		</li>
        <!-- Market -->
		<li class="flex items-center space-x-2">
			<%= heroicon 'check-circle', class: "w-6 h-6 flex-none #{I18n.locale == :he ? 'ml-2' : ''}" %>
			<%= tag.span t('home_page.top_features.market')%>
		</li>
        <!-- Easy use -->
		<li class="flex items-center space-x-2">
			<%= heroicon 'check-circle', class: "w-6 h-6 flex-none #{I18n.locale == :he ? 'ml-2' : ''}" %>
			<%= tag.span t('home_page.top_features.use')%>
		</li>
        <!-- Improvement -->
		<li class="flex items-center space-x-2">
			<%= heroicon 'check-circle', class: "w-6 h-6 flex-none #{I18n.locale == :he ? 'ml-2' : ''}" %>
			<%= tag.span t('home_page.top_features.improvement')%>
		</li>
	</ul>
</div>